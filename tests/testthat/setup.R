

if (Sys.getenv("rebrickable_key") != "") {
  .rebrickable_key <- Sys.getenv("rebrickable_key")
}

if (Sys.getenv("brickset_key") != "") {
  .brickset_key <- Sys.getenv("brickset_key")
}

if (Sys.getenv("brickset_username") != "") {
  .brickset_username <- Sys.getenv("brickset_username")
}

if (Sys.getenv("brickset_password") != "") {
  .brickset_password <- Sys.getenv("brickset_password")
}